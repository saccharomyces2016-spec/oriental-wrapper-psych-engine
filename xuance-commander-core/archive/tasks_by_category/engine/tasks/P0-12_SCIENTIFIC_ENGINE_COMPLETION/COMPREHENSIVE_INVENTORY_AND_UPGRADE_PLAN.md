# 全面盤點與升級規劃報告

**建立日期**：2026-01-12  
**目的**：全面盤點所有題庫相關內容，為下一階段大升級和大檢討做準備  
**文件狀態**：COMPLETE

---

## ✅ 根目錄清理完成

### 已刪除的壓縮檔

- ✅ `P0-12_GATE1_UPDATED_WORKSPACE.zip` - 已刪除（0.43 MB）
- ✅ `P0-12_FINAL_DELIVERY.zip` - 已刪除（0.06 MB）

**說明**：
- 所有內容已整合到專案中（`domain/` 目錄）
- 備份已保存在 `EXTRACTION_WORKSPACE/` 和 `GATE4_BACKUP/`
- 根目錄已清理完成

---

## 📊 題庫相關內容完整盤點

### 1. 題目設計（Questions）

#### 位置

**主要位置**：
- `xuance-commander-core/domain/questions/` - 所有主題的題目檔案

**檔案格式**：
- `{facetId}.questions.v1.0.json`

**檔案數量**：14 個主題

#### 統計資料

**總題目數**：75 題

**各主題題目數**：
- `avoidance_coping`: 7 題
- `chronic_alertness`: 7 題
- `chronic_depletion`: 7 題
- `emotional_permeability`: 7 題
- `fear_based_stability`: 3 題
- `hyper_responsibility`: 7 題
- `identity_diffusion`: 7 題
- `loss_of_agency`: 7 題
- `meaning_vacuum`: 7 題
- `self_erosion`: 0 題（MISSING）
- `social_comparison`: 7 題
- `stress_recovery`: 2 題
- `suppressed_needs`: 7 題
- `unprocessed_loss`: 0 題（MISSING）

**平均題目數**：5.4 題/主題

#### 題型設計

**當前題型**：
- **唯一類型**：`likert_5`（5 點李克特量表）
- **選項數量**：多數為 4 個選項（部分為 5 個）

**題目結構**：
```json
{
  "id": "chronic_depletion_q1",
  "text": "題目文字",
  "type": "likert_5",
  "scale": ["選項1", "選項2", "選項3", "選項4"]
}
```

**設計規範**：
- 參考文件：`domain/knowledge_base/question_design_guidelines.v1.0.md`
- 設計原則：玄學語境、去問卷化、單一物件選擇

**設計階段**（根據 question_design_guidelines）：
- **Stage 1（八卦錨定）**：引導使用者從八個具象象徵中選擇
- **Stage 2（五行狀態）**：描述五種能量狀態
- **Stage 3（深度剖析）**：觸發強度權重（Intensity Score）

**注意**：當前實作中未發現 Stage 1/2/3 的實際使用，所有題目都是簡單的 `likert_5` 類型

---

### 2. 分數計算方式（Scoring）

#### 位置

**主要位置**：
- `xuance-commander-core/domain/facets/` - 所有主題的分數計算檔案

**檔案格式**：
- `{facetId}.scoring.v1.0.json`

**檔案數量**：9 個主題（5 個主題缺失）

#### 分數計算結構

**計算模型**：`weighted_sum`（加權總和）

**結構範例**：
```json
{
  "model": "weighted_sum",
  "inputs": [
    {
      "questionId": "cd_q1",
      "weight": 1.2,
      "direction": "higher_is_worse"
    }
  ],
  "bands": [
    { "band": "low", "min": 0.0, "max": 0.33 },
    { "band": "mid", "min": 0.33, "max": 0.66 },
    { "band": "high", "min": 0.66, "max": 1.01 }
  ]
}
```

**計算邏輯**（根據 `engine/score_facet.py`）：
1. 對每個問題的答案進行正規化（0-1）
2. 根據 `direction` 決定是否反轉（`higher_is_worse` 或 `higher_is_worse_reversed`）
3. 加權總和計算平均分數
4. 根據 `bands` 區分等級（low/mid/high）

**缺失的 Scoring 檔案**（5 個）：
1. `social_comparison.scoring.v1.0.json`
2. `self_erosion.scoring.v1.0.json`
3. `emotional_permeability.scoring.v1.0.json`
4. `avoidance_coping.scoring.v1.0.json`
5. `unprocessed_loss.scoring.v1.0.json`

---

### 3. 問答階段設計（Stages）

#### 位置

**主要位置**：
- `xuance-commander-core/domain/narratives/` - 敘述檔案（包含階段設計）

**檔案格式**：
- `{facetId}.narr.v1.0.json`

**檔案數量**：14 個主題

#### 階段結構

**當前階段設計**：
- **階段數**：3 個階段（low、mid、high）
- **對應關係**：階段對應分數等級
- **內容結構**：每個階段包含 `opening`（開場）和 `explain`（解釋）

**範例結構**：
```json
{
  "low": {
    "opening": "你現在的氣場比較穩，像是五行之中「土」有根。",
    "explain": "土穩代表你有基本節奏，身心不容易被外界帶著跑。"
  },
  "mid": {
    "opening": "你最近像是「木」被風拉扯，能長但也容易累。",
    "explain": "木主伸展，代表你在撐著往前，但恢復沒跟上。"
  },
  "high": {
    "opening": "你目前像是「火」過旺，亮但消耗很快。",
    "explain": "火旺常見於壓力堆疊，短期能衝，長期容易透支。"
  }
}
```

**設計規範中的階段**（根據 question_design_guidelines）：
- **Stage 1（八卦錨定）**：未在當前實作中發現
- **Stage 2（五行狀態）**：未在當前實作中發現
- **Stage 3（深度剖析）**：未在當前實作中發現

**注意**：設計規範中提到的 Stage 1/2/3 與當前實作的 low/mid/high 階段不同

---

### 4. 底層運算引擎（Engine）

#### 位置

**主要位置**：
- `xuance-commander-core/engine/` - 引擎核心程式碼

#### 引擎組件

**主要檔案**（6 個）：
1. `compile_domain.py` - 編譯 domain 資料（1346 bytes）
   - 功能：讀取 manifest，編譯成 compiled facet
   - 處理：questions、scoring、recommendations、narratives、riskchains

2. `score_facet.py` - 分數計算引擎（1523 bytes）
   - 功能：根據 answers 計算分數和等級
   - 邏輯：加權總和、正規化、等級區分

3. `hexagram_deriver.py` - 卦象推導（928 bytes）
   - 功能：從使用者輸入推導卦象 ID 和變爻
   - 邏輯：八卦映射、變爻計算

4. `collision_calculator.py` - 衝突計算（1183 bytes）
   - 功能：計算元素關係（相生/相剋）
   - 邏輯：五行關係表、卦象元素映射

5. `root_element_mapper.py` - 根元素映射（791 bytes）
   - 功能：從出生日期計算根元素（五行）
   - 邏輯：天干對應五行

6. `__init__.py` - 套件標記（24 bytes）

**總程式碼行數**：約 191 行

#### 引擎架構

**設計理念**（根據 ADR_0001）：
- 引擎 code 固定且精簡
- 內容（questions/scoring/reco/narr/risk）全部外置化到 domain/
- 用 manifest 指向各分檔
- build 時 compile 成 compiled facet

**處理流程**：
1. Manifest → 指向各分檔
2. Compile → 編譯成 compiled facet
3. Score → 根據 answers 計算分數和等級

---

### 5. 其他相關內容

#### Recommendations（建議）

**位置**：`domain/recommendations/`
**數量**：14 個主題
**結構**：`low`、`mid`、`high` 三個層級的建議陣列

#### Riskchains（風險鏈）

**位置**：`domain/riskchains/`
**數量**：14 個主題
**結構**：`mid`、`high` 兩個層級的風險鏈陣列

#### Manifests（索引）

**位置**：`domain/manifests/`
**數量**：14 個主題
**功能**：指向各分檔的索引檔案

#### Knowledge Base（知識庫）

**位置**：`domain/knowledge_base/`
**內容**：
- `question_design_guidelines.v1.0.md` - 題目設計指南
- `bagua_8_trigrams.v1.0.json` - 八卦對應
- `wuxing_5_elements.v1.0.json` - 五行對應
- `hexagram_64_full.v1.0.json` - 64 卦對應
- `vocabulary_metaphysical.v1.0.json` - 玄學詞彙庫
- `vocabulary_psychology_mapping.v1.0.json` - 心理學映射
- 其他設計規範和對照表

---

## 📁 完整檔案結構

### Domain 目錄結構

```
domain/
├── manifests/          # 14 個 manifest 檔案（索引）
├── questions/          # 14 個 questions 檔案（題目設計）
├── narratives/         # 14 個 narratives 檔案（問答階段設計）
├── recommendations/    # 14 個 recommendations 檔案（建議）
├── riskchains/         # 14 個 riskchains 檔案（風險鏈）
├── facets/             # 9 個 scoring 檔案（分數計算，5 個缺失）
├── compiled/           # 編譯後的檔案
└── knowledge_base/     # 知識庫和設計指南
    ├── question_design_guidelines.v1.0.md
    ├── bagua_8_trigrams.v1.0.json
    ├── wuxing_5_elements.v1.0.json
    ├── hexagram_64_full.v1.0.json
    └── ...
```

### Engine 目錄結構

```
engine/
├── compile_domain.py      # 編譯 domain 資料
├── score_facet.py         # 分數計算引擎
├── hexagram_deriver.py    # 卦象推導
├── collision_calculator.py # 衝突計算
├── root_element_mapper.py  # 根元素映射
└── __init__.py
```

### 檔案統計

| 類別 | 數量 | 狀態 | 說明 |
|------|------|------|------|
| Manifests | 14 | ✅ 完整 | 所有主題都有索引 |
| Questions | 14 | ⚠️ 2 個空 | 75 題總計，2 個主題為空 |
| Narratives | 14 | ✅ 完整 | 所有主題都有敘述 |
| Recommendations | 14 | ✅ 完整 | 所有主題都有建議 |
| Riskchains | 14 | ✅ 完整 | 所有主題都有風險鏈 |
| Scoring | 9 | ⚠️ 5 個缺失 | 需要補齊 |

---

## 🔍 詳細分析

### 題型設計分析

**當前狀態**：
- **唯一類型**：`likert_5`（5 點李克特量表）
- **選項數量**：多數為 4 個選項（部分為 5 個）
- **題目結構**：統一格式（id、text、type、scale）

**設計規範中的要求**（根據 question_design_guidelines）：
- **Stage 1（八卦錨定）**：引導使用者從八個具象象徵中選擇
- **Stage 2（五行狀態）**：描述五種能量狀態
- **Stage 3（深度剖析）**：觸發強度權重

**差距分析**：
- ❌ 當前實作未使用 Stage 1/2/3 設計
- ❌ 所有題目都是簡單的 `likert_5` 類型
- ❌ 未實現設計規範中的多階段問答流程

**優點**：
- ✅ 結構統一，易於處理
- ✅ 所有主題使用相同題型，一致性高

**限制**：
- ❌ 缺乏題型多樣性
- ❌ 未實現設計規範中的多階段設計
- ❌ 可能限制題目設計靈活性

---

### 分數計算分析

**當前計算方式**：
- **模型**：`weighted_sum`（加權總和）
- **方法**：正規化答案（0-1），加權總和，等級區分
- **等級**：3 個等級（low、mid、high）
- **方向**：`higher_is_worse` 或 `higher_is_worse_reversed`

**計算邏輯**（根據 `engine/score_facet.py`）：
```python
# 正規化答案（0-1）
score = v / 4.0  # 或 (4.0 - v) / 4.0（反轉）

# 加權總和
total += score * weight
total_w += weight

# 平均分數
avg = total / total_w

# 等級區分
if avg >= 0.0 and avg < 0.33: band = "low"
elif avg >= 0.33 and avg < 0.66: band = "mid"
elif avg >= 0.66 and avg < 1.01: band = "high"
```

**優點**：
- ✅ 計算方式簡單直接
- ✅ 易於理解和維護
- ✅ 所有主題使用相同計算邏輯

**限制**：
- ❌ 所有主題使用相同計算邏輯，缺乏個性化
- ❌ 5 個主題缺失 scoring 檔案
- ❌ 固定閾值（0.33、0.66），缺乏彈性

---

### 問答階段分析

**當前階段設計**：
- **階段數**：3 個階段（low、mid、high）
- **內容結構**：opening + explain
- **對應關係**：階段對應分數等級

**設計規範中的階段**（根據 question_design_guidelines）：
- **Stage 1（八卦錨定）**：未實作
- **Stage 2（五行狀態）**：未實作
- **Stage 3（深度剖析）**：未實作

**差距分析**：
- ❌ 當前實作只有結果階段（low/mid/high），沒有問答階段（Stage 1/2/3）
- ❌ 未實現設計規範中的多階段問答流程
- ❌ 缺乏動態調整機制

**優點**：
- ✅ 階段設計與分數等級對應，邏輯清晰
- ✅ 結構統一

**限制**：
- ❌ 只有 3 階段設計，較為簡單
- ❌ 未實現設計規範中的多階段問答
- ❌ 缺乏動態調整機制

---

### 底層運算引擎分析

**當前引擎架構**：
- **設計理念**：內容外置化（ADR_0001）
- **核心功能**：編譯 domain 資料、計算分數、推導卦象
- **處理流程**：manifest → compile → compiled facet → score

**引擎組件**：
1. **編譯引擎**（`compile_domain.py`）
   - 功能：讀取 manifest，編譯成 compiled facet
   - 處理：questions、scoring、recommendations、narratives、riskchains

2. **分數計算引擎**（`score_facet.py`）
   - 功能：根據 answers 計算分數和等級
   - 邏輯：加權總和、正規化、等級區分

3. **卦象推導引擎**（`hexagram_deriver.py`）
   - 功能：從使用者輸入推導卦象 ID 和變爻
   - 邏輯：八卦映射、變爻計算

4. **衝突計算引擎**（`collision_calculator.py`）
   - 功能：計算元素關係（相生/相剋）
   - 邏輯：五行關係表、卦象元素映射

5. **根元素映射引擎**（`root_element_mapper.py`）
   - 功能：從出生日期計算根元素（五行）
   - 邏輯：天干對應五行

**優點**：
- ✅ 引擎 code 固定且精簡（約 191 行）
- ✅ 內容與邏輯分離，易於維護
- ✅ 易於擴充新主題

**限制**：
- ⚠️ 功能較為簡單
- ⚠️ 可能需要效能優化
- ⚠️ 未實現設計規範中的多階段問答流程

---

## ⚠️ 發現的問題和改進機會

### 1. 缺失的 Scoring 檔案（高優先級）

**問題**：5 個主題缺少 scoring 檔案

**影響**：
- 無法計算這些主題的分數
- 無法區分等級（low/mid/high）
- 系統不完整

**建議**：
- 優先建立缺失的 scoring 檔案
- 統一 scoring 設計規範

---

### 2. 缺失的 Questions 內容（高優先級）

**問題**：2 個主題缺少 questions 內容

**影響**：
- `self_erosion`: 0 題
- `unprocessed_loss`: 0 題

**建議**：
- 優先補齊缺失的 questions 內容

---

### 3. 題型單一（中優先級）

**問題**：所有題目都使用 `likert_5` 類型

**影響**：
- 缺乏題型多樣性
- 未實現設計規範中的多階段問答（Stage 1/2/3）
- 可能限制題目設計靈活性

**建議**：
- 評估是否需要其他題型
- 設計多樣化的題型系統
- 實現設計規範中的多階段問答流程

---

### 4. 階段設計簡單（中優先級）

**問題**：只有 3 階段設計（low/mid/high），未實現設計規範中的 Stage 1/2/3

**影響**：
- 問答流程可能較為簡單
- 未實現設計規範中的多階段問答
- 缺乏動態調整機制

**建議**：
- 評估是否需要實現設計規範中的 Stage 1/2/3
- 設計更靈活的階段系統

---

### 5. 分數計算方式單一（中優先級）

**問題**：所有主題使用相同的計算邏輯

**影響**：
- 缺乏個性化計算方式
- 可能無法滿足不同主題的特殊需求

**建議**：
- 評估是否需要更複雜的計算邏輯
- 設計更靈活的分數計算系統

---

### 6. 設計規範與實作不一致（中優先級）

**問題**：設計規範中提到 Stage 1/2/3，但實作中未使用

**影響**：
- 設計規範與實際實作不一致
- 可能導致後續開發困惑

**建議**：
- 評估是否需要實現設計規範中的 Stage 1/2/3
- 或更新設計規範以反映當前實作

---

## 🎯 下一階段任務規劃

### 任務 1：補齊缺失的 Scoring 檔案（優先級：高）

**目標**：建立 5 個缺失的 scoring 檔案

**缺失的主題**：
1. `social_comparison.scoring.v1.0.json`
2. `self_erosion.scoring.v1.0.json`
3. `emotional_permeability.scoring.v1.0.json`
4. `avoidance_coping.scoring.v1.0.json`
5. `unprocessed_loss.scoring.v1.0.json`

**步驟**：
1. 分析現有 scoring 檔案的設計模式
2. 參考現有 scoring 檔案（如 `stress_recovery.scoring.v1.0.json`、`chronic_depletion.scoring.v1.0.json`）
3. 為每個缺失的主題建立 scoring 檔案
4. 驗證 scoring 邏輯正確性
5. 更新 manifest 檔案（如需要）

**預估工作量**：1-2 天

**交付物**：
- 5 個 scoring 檔案
- Scoring 設計規範文件

---

### 任務 2：補齊缺失的 Questions 內容（優先級：高）

**目標**：補齊 2 個主題的 questions 內容

**缺失的主題**：
1. `self_erosion` - 0 題
2. `unprocessed_loss` - 0 題

**步驟**：
1. 分析現有 questions 的設計模式
2. 參考設計規範（`question_design_guidelines.v1.0.md`）
3. 為每個缺失的主題建立 questions 內容
4. 驗證 questions 結構正確性

**預估工作量**：2-3 天

**交付物**：
- 2 個 questions 檔案
- Questions 設計規範文件

---

### 任務 3：題型系統升級（優先級：中）

**目標**：設計和實作多樣化的題型系統

**當前狀態**：
- 所有題目都使用 `likert_5` 類型
- 未實現設計規範中的 Stage 1/2/3

**需求分析**：
1. 評估需要哪些題型
2. 分析設計規範中的 Stage 1/2/3 是否需要實現
3. 設計新題型系統架構

**設計方案**：
1. 定義題型系統架構
2. 設計題型 Schema
3. 實作題型驗證邏輯

**實作步驟**：
1. 建立題型系統架構
2. 實作新題型支援
3. 遷移現有題目（如需要）
4. 測試和驗證

**預估工作量**：1-2 週

**交付物**：
- 題型系統架構文件
- 題型 Schema
- 題型驗證邏輯
- 遷移指南

---

### 任務 4：階段系統升級（優先級：中）

**目標**：設計和實作更靈活的階段系統

**當前狀態**：
- 只有 3 階段設計（low、mid、high）
- 未實現設計規範中的 Stage 1/2/3

**需求分析**：
1. 評估是否需要實現設計規範中的 Stage 1/2/3
2. 分析當前階段設計的限制
3. 設計新階段系統架構

**設計方案**：
1. 定義階段系統架構
2. 設計階段 Schema
3. 實作階段處理邏輯

**實作步驟**：
1. 建立階段系統架構
2. 實作新階段支援
3. 遷移現有階段設計（如需要）
4. 測試和驗證

**預估工作量**：1-2 週

**交付物**：
- 階段系統架構文件
- 階段 Schema
- 階段處理邏輯
- 遷移指南

---

### 任務 5：分數計算系統升級（優先級：中）

**目標**：設計和實作更靈活的分數計算系統

**當前狀態**：
- 使用 `weighted_sum` 模型
- 所有主題使用相同計算邏輯

**需求分析**：
1. 評估當前計算方式的限制
2. 分析是否需要更複雜的計算邏輯
3. 設計新計算系統架構

**設計方案**：
1. 定義計算系統架構
2. 設計計算邏輯 Schema
3. 實作計算引擎

**實作步驟**：
1. 建立計算系統架構
2. 實作新計算邏輯
3. 遷移現有計算方式（如需要）
4. 測試和驗證

**預估工作量**：1-2 週

**交付物**：
- 計算系統架構文件
- 計算邏輯 Schema
- 計算引擎
- 遷移指南

---

### 任務 6：引擎優化（優先級：低）

**目標**：優化底層運算引擎

**當前狀態**：
- 引擎 code 固定且精簡（約 191 行）
- 內容與邏輯分離

**需求分析**：
1. 效能分析：評估當前引擎效能
2. 識別效能瓶頸
3. 設計優化方案

**優化方案**：
1. 程式碼優化
2. 架構改進
3. 快取機制
4. 並行處理

**實作步驟**：
1. 效能測試和基準測試
2. 實作優化
3. 測試和驗證
4. 效能對比

**預估工作量**：2-3 週

**交付物**：
- 效能分析報告
- 優化後的引擎
- 效能對比報告

---

### 任務 7：設計規範與實作對齊（優先級：中）

**目標**：對齊設計規範與實際實作

**當前狀態**：
- 設計規範中提到 Stage 1/2/3，但實作中未使用
- 設計規範與實際實作不一致

**需求分析**：
1. 評估是否需要實現設計規範中的 Stage 1/2/3
2. 或更新設計規範以反映當前實作
3. 確保設計規範與實作一致

**實作步驟**：
1. 分析設計規範與實作的差距
2. 決定對齊策略（實現規範或更新規範）
3. 執行對齊
4. 驗證一致性

**預估工作量**：1 週

**交付物**：
- 對齊報告
- 更新後的設計規範（如需要）
- 或實現的 Stage 1/2/3（如需要）

---

## 📊 任務優先級總覽

| 任務 | 優先級 | 狀態 | 預估工作量 |
|------|--------|------|-----------|
| 補齊缺失的 Scoring 檔案 | 高 | 待執行 | 1-2 天 |
| 補齊缺失的 Questions 內容 | 高 | 待執行 | 2-3 天 |
| 題型系統升級 | 中 | 待規劃 | 1-2 週 |
| 階段系統升級 | 中 | 待規劃 | 1-2 週 |
| 分數計算系統升級 | 中 | 待規劃 | 1-2 週 |
| 設計規範與實作對齊 | 中 | 待規劃 | 1 週 |
| 引擎優化 | 低 | 待規劃 | 2-3 週 |

---

## 📝 執行建議

### 短期（1-2 週）

1. **補齊缺失的 Scoring 檔案**
   - 優先處理，影響系統完整性
   - 工作量小，可以快速完成

2. **補齊缺失的 Questions 內容**
   - 優先處理，影響系統完整性
   - 工作量中等

---

### 中期（1-2 個月）

1. **題型系統升級**
   - 設計和實作多樣化題型
   - 實現設計規範中的 Stage 1/2/3（如需要）

2. **階段系統升級**
   - 設計和實作更靈活的階段系統

3. **分數計算系統升級**
   - 設計和實作更靈活的計算系統

4. **設計規範與實作對齊**
   - 確保設計規範與實作一致

---

### 長期（3-6 個月）

1. **引擎優化**
   - 效能優化和架構改進

---

## 📋 完整檔案清單

### Domain 檔案

**Manifests**（14 個）：
- `chronic_depletion.v1.0.json`
- `hyper_responsibility.v1.0.json`
- `fear_based_stability.v1.0.json`
- `loss_of_agency.v1.0.json`
- `social_comparison.v1.0.json`
- `suppressed_needs.v1.0.json`
- `identity_diffusion.v1.0.json`
- `chronic_alertness.v1.0.json`
- `meaning_vacuum.v1.0.json`
- `self_erosion.v1.0.json`
- `emotional_permeability.v1.0.json`
- `avoidance_coping.v1.0.json`
- `unprocessed_loss.v1.0.json`
- `stress_recovery.v1.0.json`

**Questions**（14 個，75 題總計）：
- 12 個主題有內容（75 題）
- 2 個主題為空（`self_erosion`、`unprocessed_loss`）

**Narratives**（14 個）：同上清單

**Recommendations**（14 個）：同上清單

**Riskchains**（14 個）：同上清單

**Scoring**（9 個，5 個缺失）：
- ✅ `chronic_depletion.scoring.v1.0.json`
- ✅ `hyper_responsibility.scoring.v1.0.json`
- ✅ `fear_based_stability.scoring.v1.0.json`
- ✅ `loss_of_agency.scoring.v1.0.json`
- ✅ `suppressed_needs.scoring.v1.0.json`
- ✅ `identity_diffusion.scoring.v1.0.json`
- ✅ `chronic_alertness.scoring.v1.0.json`
- ✅ `meaning_vacuum.scoring.v1.0.json`
- ✅ `stress_recovery.scoring.v1.0.json`
- ❌ `social_comparison.scoring.v1.0.json`（缺失）
- ❌ `self_erosion.scoring.v1.0.json`（缺失）
- ❌ `emotional_permeability.scoring.v1.0.json`（缺失）
- ❌ `avoidance_coping.scoring.v1.0.json`（缺失）
- ❌ `unprocessed_loss.scoring.v1.0.json`（缺失）

### Engine 檔案

**主要檔案**（6 個）：
- `compile_domain.py` - 編譯 domain 資料
- `score_facet.py` - 分數計算引擎
- `hexagram_deriver.py` - 卦象推導
- `collision_calculator.py` - 衝突計算
- `root_element_mapper.py` - 根元素映射
- `__init__.py` - 套件標記

**總程式碼行數**：約 191 行

---

## 🎉 總結

### 已完成

- ✅ 根目錄清理完成
- ✅ 全面盤點完成
- ✅ 分析報告完成
- ✅ 任務規劃完成

### 待執行

- ⏳ 補齊缺失的 Scoring 檔案（優先級：高）
- ⏳ 補齊缺失的 Questions 內容（優先級：高）
- ⏳ 系統升級任務（優先級：中）
- ⏳ 引擎優化（優先級：低）

---

**文件狀態**：COMPLETE  
**最後更新**：2026-01-12  
**下一步**：開始執行任務 1（補齊缺失的 Scoring 檔案）
