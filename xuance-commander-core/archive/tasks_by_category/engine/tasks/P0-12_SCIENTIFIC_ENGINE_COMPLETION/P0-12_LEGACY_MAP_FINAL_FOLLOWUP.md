# P0-12 Legacy Map 最終追問包

**建立日期**：2026-01-12  
**文件狀態**：WORKING DOCUMENT（不鎖定，可回滾）  
**目的**：針對 Legacy Map 最終審核中發現的問題進行追問

---

## 一、審核結果總結

### 1.1 符合的部分 ✅

1. **JSON 結構**：
   - ✅ 結構完全正確（沒有 meta 欄位）
   - ✅ 所有 8 個領域都有完整映射
   - ✅ 所有 56 題都有完整映射
   - ✅ 所有 280 個選項都有完整映射
   - ✅ 所有映射都包含 `pattern` 和 `legacy_weight`

2. **完整性**：
   - ✅ 結構完整性：100%
   - ✅ 內容完整性：100%

---

### 1.2 不符合的部分 ❌

1. **權重一致性問題（嚴重）**：
   - ❌ **37 題權重不一致**（只有 9 題一致）
   - ⚠️ 顧問聲稱已修正，但實際上仍有大量問題
   - ⚠️ 顧問可能仍參考了錯誤的版本

2. **題目 ID 命名差異**：
   - ⚠️ `loss_of_agency` 領域：實際系統使用 `loa_q1`，顧問使用 `la_q1`

3. **題目數量差異**：
   - ⚠️ `fear_based_stability` 領域：實際系統只有 3 題，但顧問提供了 7 題

---

## 二、需要修正的問題

### 2.1 問題 1：權重不一致（嚴重問題）

**問題說明**：
- 顧問提供的 `legacy_weight` 與實際系統中的 scoring 文件權重存在**37 題不一致**
- 顧問聲稱已修正，但實際上仍有大量問題
- 顧問可能仍參考了錯誤的版本（v3.0 資料包，而非實際系統）

**正確要求**：
- `legacy_weight` 必須與**實際系統中的 scoring 文件**中的 `weight` 完全一致
- **權威來源**：`domain/facets/{domain_id}.scoring.v1.0.json`
- 每個題目的所有選項（choice 0-4）使用相同的 `legacy_weight`（對應題目級別的權重）

**實際系統中的權重（權威來源）**：

以下權重來自實際系統中的 `domain/facets/{domain_id}.scoring.v1.0.json` 文件，**這是權威來源，必須以此為準**。

#### chronic_depletion

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| cd_q1 | 1.2 |
| cd_q2 | 1.1 |
| cd_q3 | 1.3 |
| cd_q4 | 1.1 |
| cd_q5 | 1.2 |
| cd_q6 | 1.0 |
| cd_q7 | 1.2 |

#### chronic_alertness

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| ca_q1 | 1.2 |
| ca_q2 | 1.1 |
| ca_q3 | 1.1 |
| ca_q4 | 1.2 |
| ca_q5 | 1.3 |
| ca_q6 | 1.1 |
| ca_q7 | 1.2 |

#### fear_based_stability

**注意**：實際系統只有 3 題。

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| fbs_q1 | 1.2 |
| fbs_q2 | 1.0 |
| fbs_q3 | 1.2 |

#### hyper_responsibility

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| hr_q1 | 1.2 |
| hr_q2 | 1.3 |
| hr_q3 | 1.1 |
| hr_q4 | 1.2 |
| hr_q5 | 1.4 |
| hr_q6 | 1.2 |
| hr_q7 | 1.1 |

#### identity_diffusion

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| id_q1 | 1.3 |
| id_q2 | 1.1 |
| id_q3 | 1.2 |
| id_q4 | 1.2 |
| id_q5 | 1.3 |
| id_q6 | 1.1 |
| id_q7 | 1.2 |

#### loss_of_agency

**注意**：實際系統使用 `loa_q1`（不是 `la_q1`）。

| 題目 ID（實際系統） | Scoring Weight（實際系統） |
|------------------|-------------------------|
| loa_q1 | 1.2 |
| loa_q2 | 1.4 |
| loa_q3 | 1.3 |
| loa_q4 | 1.2 |
| loa_q5 | 1.2 |
| loa_q6 | 1.1 |
| loa_q7 | 1.2 |

#### meaning_vacuum

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| mv_q1 | 1.2 |
| mv_q2 | 1.3 |
| mv_q3 | 1.2 |
| mv_q4 | 1.1 |
| mv_q5 | 1.2 |
| mv_q6 | 1.3 |
| mv_q7 | 1.1 |

#### suppressed_needs

| 題目 ID | Scoring Weight（實際系統） |
|---------|-------------------------|
| sn_q1 | 1.2 |
| sn_q2 | 1.3 |
| sn_q3 | 1.1 |
| sn_q4 | 1.2 |
| sn_q5 | 1.4 |
| sn_q6 | 1.3 |
| sn_q7 | 1.1 |

---

### 2.2 問題 2：題目 ID 命名差異

**問題說明**：
- `loss_of_agency` 領域：實際系統使用 `loa_q1`，但顧問使用 `la_q1`
- 這會導致映射無法正確匹配

**正確要求**：
- 必須使用實際系統中的題目 ID
- `loss_of_agency` 領域必須使用 `loa_q1`, `loa_q2`, ..., `loa_q7`（不是 `la_q1`）

---

### 2.3 問題 3：題目數量差異

**問題說明**：
- `fear_based_stability` 領域：實際系統只有 3 題（fbs_q1, fbs_q2, fbs_q3），但顧問提供了 7 題
- 需要確認：是否應該只有 3 題，還是實際系統不完整？

**正確要求**：
- 必須確認實際系統中的題目數量
- 如果實際系統只有 3 題，則 legacy_map 也應該只有 3 題的映射

---

## 三、關鍵說明

### 3.1 權威來源

**重要**：`legacy_weight` 必須與**實際系統中的 scoring 文件**一致。

**實際系統中的 scoring 文件位置**：
- `domain/facets/{domain_id}.scoring.v1.0.json`

**不是**：
- ❌ v3.0 資料包中的 scoring 文件
- ❌ 其他版本的 scoring 文件

**原因**：
- 實際系統中的 scoring 文件是當前系統的權威來源
- 這些文件已經被寫入系統並使用
- `legacy_weight` 必須與這些文件的權重一致

---

### 3.2 修正步驟

1. **讀取實際系統中的 scoring 文件**：
   - 從 `domain/facets/` 目錄讀取所有領域的 scoring 文件

2. **提取權重**：
   - 從 `inputs` 陣列中提取每個題目的 `weight`

3. **更新 legacy_map**：
   - 將所有題目的所有選項（choice 0-4）的 `legacy_weight` 更新為對應的權重

4. **驗證**：
   - 確保所有權重都與實際系統中的 scoring 文件一致
   - 確保題目 ID 正確（特別是 `loss_of_agency` 使用 `loa_q1`）

---

## 四、追問要求

### 4.1 必須修正的問題

1. **修正所有權重不一致問題（37 題）**：
   - 必須參考實際系統中的 scoring 文件
   - 所有權重必須與實際系統完全一致

2. **修正題目 ID 命名**：
   - `loss_of_agency` 領域必須使用 `loa_q1`（不是 `la_q1`）

3. **確認題目數量**：
   - `fear_based_stability` 領域：確認應該有多少題目

---

### 4.2 驗證要求

**通過標準**：
1. ✅ JSON 結構正確（沒有 meta 欄位）
2. ✅ 所有 8 個領域都有完整映射
3. ✅ 每個領域的所有題目都有完整映射
4. ✅ 每個題目的所有 5 個選項（choice 0-4）都有映射
5. ✅ 所有映射都包含 `pattern` 和 `legacy_weight`
6. ✅ 所有 `legacy_weight` 都與**實際系統中的 scoring 文件**中的 `weight` 一致
7. ✅ 題目 ID 正確（特別是 `loss_of_agency` 使用 `loa_q1`）

---

**文件狀態**：WORKING DOCUMENT（不鎖定，可回滾）  
**最後更新**：2026-01-12  
**當前狀態**：❌ **需要修正 37 題的權重不一致問題，以及題目 ID 命名差異**
